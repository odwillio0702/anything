const $=s=>document.querySelector(s), $$=s=>document.querySelectorAll(s);const body=document.body;$('#year').textContent=new Date().getFullYear();
const saved=localStorage.getItem('theme');if(saved==='dark')body.classList.add('dark');const theme=$('.theme-toggle');theme.textContent=body.classList.contains('dark')?'☀':'☾';theme.onclick=()=>{body.classList.toggle('dark');localStorage.setItem('theme',body.classList.contains('dark')?'dark':'light');theme.textContent=body.classList.contains('dark')?'☀':'☾'};
const menu=$('.menu-toggle'),nav=$('.nav-links');menu.onclick=()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',open)};$$('.nav-links a').forEach(a=>a.onclick=()=>nav.classList.remove('open'));
const observer=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add('visible')),{threshold:.12});$$('.reveal').forEach(e=>observer.observe(e));
$$('.filter').forEach(button=>button.onclick=()=>{$$('.filter').forEach(b=>b.classList.remove('active'));button.classList.add('active');const filter=button.dataset.filter;$$('.project').forEach(p=>p.classList.toggle('hidden',filter!=='all'&&p.dataset.category!==filter))});
$('#contact-form').onsubmit=e=>{e.preventDefault();const form=e.target;if(!form.checkValidity()){form.reportValidity();return}$('.form-status').textContent='Thanks — your message is ready to send. Connect this form to a backend or form service to receive submissions.';form.reset()};
$('#download-cv').onclick=()=>{const cv=`YOUR NAME\nProduct Designer & Front-End Developer\n\nABOUT\nReplace this document with your CV details.\n\nEXPERIENCE\nSenior Product Designer — Company Name\nProduct Designer — Studio Name\n\nSKILLS\nProduct design | Web development | Design systems`;const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([cv],{type:'text/plain'}));a.download='your-name-cv.txt';a.click();URL.revokeObjectURL(a.href)};
const dot=$('.cursor-dot'),ring=$('.cursor-ring');document.addEventListener('mousemove',e=>{dot.style.left=e.clientX+'px';dot.style.top=e.clientY+'px';ring.style.left=e.clientX+'px';ring.style.top=e.clientY+'px'});
